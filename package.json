{
  "name": "dhronavaradhi-monorepo",
  "private": true,
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && npm start",
    "dev": "concurrently \"cd backend && npm run start\" \"cd frontend && npm start\""
  }
}
